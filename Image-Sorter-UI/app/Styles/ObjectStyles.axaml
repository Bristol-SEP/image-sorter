<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Design.PreviewWith>
        <Border Padding="20">
        </Border>
    </Design.PreviewWith>

    <!-- button style -->
    <Style Selector="Button.baseButton">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="CornerRadius" Value="5"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Foreground" Value="{DynamicResource Text}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"></Setter>
        <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
        <Setter Property="Background" Value="{DynamicResource Buttons}"/>
    </Style>
    <Style Selector="Button:pointerover /template/ ContentPresenter">
        <Setter Property="Background" Value="{DynamicResource ButtonSelected}"/>
        <Setter Property="Foreground" Value="{DynamicResource Text}"/>
    </Style>
    
   <!-- Check box style  -->
    <Style Selector="CheckBox.baseCheckBox">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Foreground" Value="{DynamicResource Text}"/>
    </Style>
    <Style Selector="CheckBox.baseCheckBox /template/ Border#NormalRectangle">
        <Setter Property="BorderBrush" Value="{DynamicResource Text}"/> 
    </Style>
    <Style Selector="CheckBox.baseCheckBox:pointerover /template/ ContentPresenter">
        <Setter Property="Foreground" Value="{DynamicResource Text}"/>
    </Style>
    
    <!-- Alert style -->
    <Style Selector="TextBlock.alert">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource Alert}"/>
        <Setter Property="Margin" Value="5,0,0,0"/>
    </Style>
    <!-- Text Box style -->
    <Style Selector="TextBox.baseTextBox">
        <Setter Property="Background" Value="{DynamicResource BackgroundWeak}"/>
        <Setter Property="Foreground" Value="{DynamicResource Text}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BackgroundStrong}"/>
        <Setter Property="Margin" Value="5,2"/>
    </Style>
    <Style Selector="TextBox.baseTextBox:pointerover /template/ Border#PART_BorderElement">
        <Setter Property="Background" Value="{DynamicResource BackgroundStrong}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BackgroundStrong}"/>
    </Style>
    <Style Selector="TextBox.baseTextBox:focus /template/ Border#PART_BorderElement">
        <Setter Property="Background" Value="{DynamicResource BackgroundStrong}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BackgroundStrong}"/>
    </Style>
    
    <!-- Slider Style -->
    <Style Selector="Slider.defaultSlider">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="10,0"/>
    </Style>
    <Style Selector="Slider.defaultSlider /template/ Thumb">
        <Setter Property="Background" Value="{DynamicResource BackgroundStrong}"/>
    </Style>
    <Style Selector="Slider.defaultSlider:pressed /template/ Thumb">
        <Setter Property="Background" Value="{DynamicResource Text}"/>
    </Style>
    <Style Selector="Slider.defaultSlider /template/ RepeatButton#PART_IncreaseButton">
        <Setter Property="Background" Value="{DynamicResource BackgroundStrong}"/>
    </Style>
    <Style Selector="Slider.defaultSlider /template/ RepeatButton#PART_DecreaseButton">
        <Setter Property="Background" Value="{DynamicResource Text}"/>
    </Style>
    
    <!-- Expander style -->
    <Style Selector="Expander.defaultExpander">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Background" Value="{DynamicResource BackgroundWeak}"/>
    </Style>
</Styles>
